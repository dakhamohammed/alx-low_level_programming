#!/usr/bin/python3
"""module that returns the perimeter of the island described in grid"""


def island_perimeter(grid):
    """method that returns the perimeter of island described in grid """
    perimeter = 0
    rows = len(grid)

    if grid != []:
        columns = len(grid[0])

    # TODO: Continue next afternoon, ALLAH willing.

