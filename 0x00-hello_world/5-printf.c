#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/**
	 * printf function to print below statment to the terminal:
	 * with proper grammar, but the outcome is a piece of art,
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
